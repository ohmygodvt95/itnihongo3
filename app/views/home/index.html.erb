<div class="container-fluid hero" style="background-image: url(<%= asset_path "hero.jpg" %>)">
  <h1 class="text-center">旅行の場所」のレビューサイト</h1>
  <div id="namer">
    <div id="namer-input">
      <input type="text" name="namername" placeholder="Type your search">
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-sm-3"></div>
    <div class="col-sm-9">
      <div class="row">
        <div class="col-sm-12">
          <h3>あなたは何を考えていますか？ <a href="<%= new_post_path %>" class="btn btn-primary pull-right"><i class="fa fa-plus fa-fw"></i>新しいレビュー</a></h3>
        </div>
      </div>
      <div class="row">
        <hr>
        <% @posts.each do |post| %>
        <div class="col-sm-4">
          <%= link_to post do %>
            <img src="<%= post.image.thumb %>" alt="<%= post.title %>", class="img-responsive img-rounded">
          <% end %>
          <br>
          <p><%= post.title %></p>
          <h6> - by <a href="#"><%= post.user.name %></a></h6>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
